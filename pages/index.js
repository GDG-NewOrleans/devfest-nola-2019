import Layout from "../components/Layout";

export default () => (
  <Layout>
    <h1>DevFest New Orleans 2019</h1>
    <p>Big fat header and description</p>
    <p>Build tool domain singleton OTP FIFO raspberry pi frontend Ruby XML developer. Cache diversity and inclusion compile atomic design domain keycaps tabs vs spaces. Transaction streams XML quick sort private var open source. Git npm ping legacy code incognito little Bobby Tables Byzantine fault tolerance lang driver. Environment pairing security ICO antipattern whiteboard.</p>

    <h2 id="speakers">Speakers</h2>
    <p>Distributed raspberry pi shadow DOM inheritance greedy algorithm Edge XML. Containerized lazy eval DSL callback hell YAML void i engineer. Container SOAP npm frontend MacBook command-line service worker looks good to me driver. ICO waterfall kernel npm bitwise operator elixir linker. Hashtable hardcoded contribute view-model parameter static typing configuration convention node_modules injection.</p>

    <h2 id="schedule">Schedule</h2>
    <p>RPC XML static cache cherry pick greenfield pull request distributed systems architecture antipattern. Edge scalable TL i S3 chmod. First in first out progressive web app internet button free as in beer compilation behavior-driven design programmer duck typing.</p>

    <h2 id="sponsors">Sponsors</h2>
    <p>RPC XML static cache cherry pick greenfield pull request distributed systems architecture antipattern. Edge scalable TL i S3 chmod. First in first out progressive web app internet button free as in beer compilation behavior-driven design programmer duck typing.</p>
  </Layout>
)
